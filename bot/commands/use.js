module.exports = {
	name: 'use',
	description: 'this command uses the item and quantity specified on the pokemon specified.',

	// eslint-disable-next-line no-unused-vars
	execute(message, args) {
		message.channel.send('>>> This command has not been completed yet.');
	},
};
